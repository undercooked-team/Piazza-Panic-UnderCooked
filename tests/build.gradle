

apply plugin: "java"

// sourceCompatibility = 1.6
sourceCompatibility = 1.8
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets {
	test {
        java {
            srcDir 'src'
        }
    }
}

test {
    exclude "**/examples/**"
}

eclipse.project {
    name = appName + "-tests"
}
